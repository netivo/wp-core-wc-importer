# Netivo WP Core WooCommerce Built-in Import Extenstion

This module adds custom columns to the WooCommerce product export and import functionality. It allows for the export and
import of custom product meta data.